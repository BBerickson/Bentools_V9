# Global constants.
# The per-session data store (LIST_DATA) lives inside server() so each user
# session gets its own copy — see R_scripts/server.R.

# Brewer color sets to be available ----
kBrewerList <- c("Set1","Paired","Dark2","Spectral")
